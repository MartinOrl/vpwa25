{
  "name": "huddlehub",
  "version": "1.0.0",
  "description": "HuddleHub",
  "productName": "HuddleHub",
  "main": "index.js",
  "private": true,
  "workspaces": {
    "packages": [
      "frontend",
      "backend"
    ]
  },
  "scripts": {
    "web:dev": "yarn workspace frontend dev",
    "web:lint": "yarn workspace frontend lint",
    "web:format-check": "yarn workspace frontend format:check",
    "web:format": "yarn workspace frontend format",
    "web:build": "yarn workspace frontend build",
    "web:dev-pwa": "yarn workspace frontend dev:pwa",
    "dev": "concurrently \"yarn web:dev\" \"yarn workspace backend dev\"",
    "backend:dev": "yarn workspace backend dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
